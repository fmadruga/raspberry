import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    paddingBottom: 7.5,
    paddingLeft: 15,
    paddingRight: 15,
    paddingTop: 7.5,
  },
});

export default styles;
